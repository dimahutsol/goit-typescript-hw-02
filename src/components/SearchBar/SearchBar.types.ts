export type SearchBarType = {
	handleSearchSubmit: (valueToSearch: string) => void;
};

export type initialValuesType = {
	searchField: string;
};

export type valuesType = {
	searchField: string;
};

export type optionsType = {
	resetForm: () => void;
};
